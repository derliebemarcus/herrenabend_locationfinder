# Deployment architecture

Marcus Pauli Portfolio Stack is deployed as Multi-container Podman stack deployed below `/srv/portfolio`. Deployment and release automation validate the repository before mutating runtime state. See the [deployment diagram](diagrams/deployment.md).
