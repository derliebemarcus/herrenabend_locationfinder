# Local development

1. Use Node.js versions compatible with the frontend and Strapi packages.
2. Run frontend lint, type checks, unit tests, build, and Playwright tests.
3. Build Strapi and validate content, locale, and search synchronization behavior.

Keep repository-specific secrets outside Git and preserve generated-file boundaries.
