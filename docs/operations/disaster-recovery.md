# Disaster recovery

1. Provision a compatible Podman host and restore host configuration.
2. Restore PostgreSQL, uploads, and exported content.
3. Deploy the repository, start the stack, synchronize search, and run smoke tests.
