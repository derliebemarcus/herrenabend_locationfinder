# Compatibility

- Node.js versions supported by the frontend and Strapi packages
- Podman, podman-compose, systemd, and nginx on the deployment host
- Modern browsers
- PostgreSQL and Meilisearch versions declared by the stack

Breaking compatibility changes require an explicit major release or migration note.
